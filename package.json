{
  "name": "u-bootstrap",
  "version": "1.0.0",
  "description": "Bootstrap grid and u-prefixed utilities for BEM fans.",
  "main": "index.js",
  "scripts": {
    "scss": "npx node-sass scss/ -o dist/css",
    "prefix": "npx postcss --config build/postcss.config.js --replace dist/css/u-bootstrap.css",
    "build": "npx npm-run-all -s scss prefix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.4.1"
  },
  "devDependencies": {
    "clean-css-cli": "^4.3.0",
    "node-sass": "^4.13.1",
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^7.1.0",
    "postcss-prefixer": "^2.1.2"
  }
}
